 removefilter(filter);
            applyAllfilter();
            const clearbtn = document.getElementById('btnclearfilter');
            clearbtn.style.display = 'none;
            
const activeFilters ={}
document.querySelectorAll('.filtercontent input[type="checkbox"]:checked, .filtercontent input[type="radio"]:checked')
        .forEach(cb => {
            const category = cb.name;
            if(!activeFilters[category]) activeFilters[category] = [];
            activeFilters[category].push(cb.value.toLowerCase());
    });